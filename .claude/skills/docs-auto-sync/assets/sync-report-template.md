# Documentation Sync Report Template

只保留与本次任务有关的段落；没有待确认项时省略该节。

```markdown
## 文档同步结果

- `<文件或章节>` → <最小修正> ← <权威依据或实现证据>
- `<文件或章节>` → 已核对，无需修改 ← <依据>

## 验证

- `<命令>`：<通过/失败及必要摘要>

## 待确认

- `<冲突或缺失证据>`：<为何不能安全自动决定；需要谁提供什么决策>
```

不要声称未运行的检查通过。若未产生 diff，明确写“未发现需要修改的文档”，并列出检查范围。
