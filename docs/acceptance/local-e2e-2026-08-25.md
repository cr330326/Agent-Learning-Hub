# 2026-08-25 本地全量端到端验收报告

## 结论

**当前工作树的本地功能与正式 Docker 镜像验收通过，并已启动本地试运行。**

正式镜像为 `agent-learning-hub:local-20260825`，镜像 ID/digest 为
`sha256:477882a3deab8b42d7430bcd21d854e60bb93e9c1df47610144dbed2d9335011`。
镜像通过 Dockerfile 的依赖安装、生产构建、生产依赖裁剪、runner 打包和导出；随后以
`--no-build` 方式在独立 Compose 项目中运行并完成 Local/Cloud 边界验证。

试运行实例使用同一镜像、全新 SQLite 命名卷和只读 `local-courses` 挂载：
`http://127.0.0.1:3345`。健康检查返回 `status=ok`、`mode=local`，容器状态为
`healthy`。本轮没有部署云端、推送镜像、使用真实 OAuth 或修改素材仓库。

仍不能宣称正式发布完成：真实 GitHub OAuth 两用户隔离、GHCR 拉取/回滚、空服务器
HTTPS、异地备份/调度/告警、生产日志复查，以及 T8.8 的目录拥有者决策仍未完成。

## 执行环境

| 项目       | 值                                                           |
| ---------- | ------------------------------------------------------------ |
| 日期       | 2026-08-25（Asia/Shanghai）                                  |
| Node / npm | v24.14.0 / 11.9.0                                            |
| Docker     | 29.6.2，Docker Desktop，aarch64 host                         |
| 生产构建   | Next.js 16.3.0；Cloud/Local `check` 均通过                   |
| 依赖审计   | `npm audit --omit=dev --audit-level=high`：0 vulnerabilities |
| 浏览器     | 本机 Google Chrome + 外部 Playwright 验收工具                |
| 测试状态   | 每个阶段使用独立临时目录、端口或 Compose 命名卷              |

Playwright 仅作为本机测试工具临时安装，不在 `code/package.json` 中登记，也未进入正式
Docker 镜像。

## 已执行验证

| 验证                                         | 结果       | 证据                                                                                       |
| -------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------ |
| `npm ci --prefix code`                       | 通过       | 依赖安装完成，npm audit 0 vulnerabilities                                                  |
| `npm run check:cloud --prefix code`          | 通过       | Prettier、lint、typecheck、内容审计 0 errors、11 个 Node 工具测试、144 个 Vitest、生产构建 |
| `npm run check:local --prefix code`          | 通过       | Local 配置下同一质量门禁全部通过                                                           |
| `npm run audit:boundaries --prefix code`     | 通过       | 缺失 ignore 规则 0、危险 CI artifact 路径 0、未授权 tracked local material 0               |
| `npm run audit:baseline --prefix code`       | 通过       | 4 tracks、9 stages、42 courses、38 groups、472 chapters、552 references                    |
| `npm run materials --prefix code -- check`   | 通过       | 54 repositories；latest 42、behind 3、diverged 2、dirty 7、missing 0                       |
| `npm run audit:materials --prefix code`      | 按设计非零 | 0 missing paths；4 个未收录仓库、479 个条目无 upstream fallback，待目录策展                |
| `npm run materials --prefix code -- audit`   | 通过       | 0 errors、2003 warnings                                                                    |
| `npm run materials --prefix code -- reindex` | 通过       | 621 documents：stage 9、item 515、project 20、local-chapter 77                             |
| `npm run drill:restore --prefix code`        | 通过       | 合成 fixture 16/16，含错误口令、篡改密文、覆盖目标和缺 `--yes` 反向对照                    |
| `git diff --check`                           | 通过       | 无 whitespace error                                                                        |

生产构建仍有 6 条 Turbopack 动态文件系统追踪 warning；它们不使构建失败，但保留为
生产评估项，未被本次验收隐藏。

## 原生生产服务器浏览器验收

使用一次性临时 SQLite 和 `with_server.py` 管理生产服务器生命周期：

- Local `seed`：通过，阅读、收藏、任务、笔记、阶段成果和主动确认；控制台 0 错误。
- Local `resume`：通过，停止并重启服务后状态、导出和删号流程通过；控制台 0 错误。
- Local `fallback`：通过，空素材目录只保留上游入口，不出现站内阅读入口；控制台 0 错误。
- Local `mobile`：通过，390×844 路线、搜索、阅读器无横向溢出；控制台 0 错误。
- `npm run test:e2e:local --prefix code`：通过，Local HTTP 状态写入、回读、导出和删号。
- `npm run test:e2e:cloud --prefix code`：通过，Cloud 29/29；GitHub token/profile 为测试桩，
  不是真实 OAuth。

首轮 fallback 暴露了验收脚本对合法的两个相同上游链接使用 strict locator 的问题；已改为
断言至少存在一个目标链接，并在原生与 Docker 两套流程中复验通过。该修正不改变产品行为。

## 正式 Docker 镜像验收

首次通过 `code/scripts/docker-deploy.sh local build` 观察到 quiet build 长时间无输出；停止的
只是独立构建进程。随后用同一个 `code/docker/Dockerfile`、同一构建上下文和可见进度重新打包：

```bash
DOCKER_BUILDKIT=1 docker build --progress=plain \
  --build-arg APP_VERSION=local-20260825 \
  -t agent-learning-hub:local-20260825 \
  -f code/docker/Dockerfile .
```

构建过程完整通过：Node 基础镜像、镜像内 `npm ci`、Next production build、`npm prune
--omit=dev`、runner 文件复制和 image export 均成功。镜像内容探针确认：

- `/app/code/content`、`/app/code/.next` 和 `/data/state` 存在；
- `/app/code/local-courses`、`/app/code/.env` 和 SQLite 文件不存在；
- Local Material 只在 Compose 运行时以 `/data/local-courses:ro` 挂载；
- 容器端口只映射到回环地址，状态写入独立命名卷。

### Docker Local

正式镜像在独立 Compose 项目中以 `--no-build` 运行，验证明确没有从源码重建：

- `127.0.0.1:3341`：浏览器 `seed`、容器重启后的 `resume`、空素材 `fallback`、`mobile`
  均通过，控制台 0 错误。
- `127.0.0.1:3343`：全新命名卷上 `test:e2e:local` 通过，避免破坏性测试护栏拦截。
- UI 走查：48 captures / 0 findings。
- 点击式功能回归：23/23。

测试过程中 `learning-state-http.mjs` 曾因 mobile 阶段自动写入 `itemProgress` 而拒绝运行；
未使用 `E2E_ALLOW_DESTRUCTIVE=1` 绕过，而是换用全新命名卷重跑并通过。这证明测试数据隔离
和删号护栏有效。

### Docker Cloud clean-room

正式镜像在本机 Cloud Compose（仅假 OAuth 配置）中以 `127.0.0.1:3344` 运行：

- `/api/health` 返回 `mode=cloud`；
- Cloud 浏览器公开流程通过，控制台 0 错误；
- 9 条公开路由、health 和管理员匿名边界 HTTP smoke 通过；
- 运行时没有挂载 `local-courses`，未执行任何真实 OAuth 或外部写入。

## AC-01—AC-10 矩阵

| 验收                   | 状态     | 本轮结论                                                           |
| ---------------------- | -------- | ------------------------------------------------------------------ |
| AC-01 Cloud 无本地素材 | 通过     | 当前正式镜像 Cloud clean-room、公开浏览、搜索和边界通过            |
| AC-02 Local 优先与回退 | 通过     | 正常素材阅读和空素材 fallback 均通过                               |
| AC-03 路径穿越防护     | 通过     | Local/Cloud 质量门禁与安全负向测试通过                             |
| AC-04 学习进度保持     | 部分通过 | Local 重启和 Docker 命名卷通过；真实 GitHub 两用户跨会话待外部验证 |
| AC-05 阶段完成约束     | 通过     | 阶段成果主动确认流程通过                                           |
| AC-06 个人数据生命周期 | 通过     | 笔记、收藏、成果、导出、删号和脱敏通过                             |
| AC-07 素材更新保护     | 部分通过 | 策略/审计通过；真实嵌套素材仓库更新未执行                          |
| AC-08 移动端完整流程   | 通过     | 390×844 路线、搜索、阅读器无横向溢出                               |
| AC-09 备份恢复         | 部分通过 | 本地合成恢复演练 16/16；异地存储和生产部署恢复待外部验证           |
| AC-10 旧站核心能力对等 | 通过     | 既有对等报告和当前质量门禁通过                                     |

## 当前未完成项

根据 [tasks.md](../plans/tasks.md) 当前状态，未完成项不能简单归结为“云端部署”：

- T7.1/T7.2/T7.3/T7.4/T7.6/T7.7：受保护 CI、GHCR/版本回滚、真实 HTTPS/OAuth、调度/异地备份、生产日志和云端流程复现；均属于外部交付门禁。
- T8.2/T8.4/T8.5：本地证据已补强，但真实 OAuth、生产日志/回滚和入口切换仍需外部环境。
- T8.8：唯一明确的非云端未完成项。当前 74 个文件被多个条目声明，需先决定课程条目和旧站单章条目的唯一拥有者，再新增 ADR 并策展，不能用自动全量声明静默制造重复搜索结果。

因此本轮结论是：**本地功能和正式镜像可以进入试运行；正式发布仍为 NO-GO；T8.8 需要目录归属决策后继续。**
