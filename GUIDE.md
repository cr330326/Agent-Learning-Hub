# 使用指南

这份文档面向**使用者**：想按九阶段路线学 Agent 工程的人，以及在本机跑起这个站点的维护者。
需求与验收以 [spec.md](docs/plans/spec.md) 为准，架构与边界以 [plan.md](docs/plans/plan.md) 为准；本文只讲“怎么用”。

---

## 一、两种运行模式

站点只有两种模式，界面右上角的徽标随时告诉你当前在哪一种。

|            | **云端模式** Cloud                       | **本地模式** Local                                 |
| ---------- | ---------------------------------------- | -------------------------------------------------- |
| 徽标       | 绿点 · 云端模式                          | 橙点 · 本地模式                                    |
| 身份       | GitHub 登录                              | 回环地址上的固定单用户，免登录                     |
| 能读什么   | 站内自有文章 + 上游链接                  | 以上全部，**再加**本机 `local-courses/` 的素材正文 |
| 第三方素材 | 只显示作者、许可证和上游地址，不复制正文 | 只读渲染，可在站内直接阅读                         |
| 学习状态   | 存在服务端 SQLite，跟随账户              | 存在本机 SQLite，属于这台机器                      |

一句话：**想真正读素材正文，用本地模式。**

---

## 二、把站点跑起来

需要 Node.js 22 或更新版本。

### 1. 安装依赖

```bash
npm ci --prefix code
```

### 2A. 本地模式（推荐，能读素材）

```bash
npm run dev:local --prefix code
```

打开 <http://127.0.0.1:3000>。右上角应显示**本地模式**。

素材目录默认取仓库根的 `local-courses/`。放在别处时指定：

```bash
LOCAL_MATERIAL_ROOT=/path/to/local-courses npm run dev:local --prefix code
```

### 2B. 云端模式（公开视角）

```bash
npm run dev:cloud --prefix code
```

这是部署到公网时的样子：第三方素材只给出处，不给正文。想验证“公开发布后别人看到什么”，用这个。

### 3. Docker 预览（最接近真实部署）

```bash
code/scripts/local-preview.sh
```

从当前代码构建镜像、启动本地模式、跑健康检查，然后打开 <http://127.0.0.1:3000>。
管理命令：`status`、`logs`、`restart`、`down`。`down` **不会**删除保存学习状态的 SQLite 卷。

> **只用 `127.0.0.1` 或 `localhost` 访问。** 本地模式的免登录身份只对回环地址成立，
> 站点在绑定到非回环地址时会直接拒绝启动。

同一 `code/` 目录同时只能跑一个 `next dev`（第二个实例会以 “server is already running” 退出），所以想在本机并行对照云端与本地两种模式，只能用 Docker 的 `code/scripts/mode-switch.sh`——用法见 [快速上手](USER.md) 第 3 节与 [README](README.md#docker)。

---

## 三、页面怎么用

### 首页 `/`

三件事：九阶段路线的入口、四条轨道的分流、云端/本地两种模式的说明。
右侧深绿卡片是目录规模速览——**课程条目 / 本地素材 / 站内文章 / 上游导览**，四个数字相加等于总数。

### 九阶段路线 `/roadmap`

主线。每一站回答三个问题：要理解什么、要动手做什么、最后留下什么可复查的成果。
点进任一阶段 `/roadmap/stage-N`，会看到三栏：

1. **LEARNING GOALS** — 这一站要能说清楚的事。
2. **PRACTICE TASKS** — 编号动作，逐条做，**可以直接在这里勾选**，勾完即时保存并划掉。
3. **PROOF OF WORK** — 深绿卡片写着这一站要交的东西，右下角直接跳到“我的学习”登记。

底部是这一站的**配套资料**，以及上一阶段 / 下一阶段导航。

回到 `/roadmap`，每一行右侧会显示你在这一站的进度：一条细进度条加一行 `动作 1/3`。三个动作全勾完时变成 `动作已做完 · 待交成果`；只有在“我的学习”里真的登记了成果，才会显示绿色的 `已交成果`。

> 云端模式未登录时看不到勾选框和进度条 —— 动作清单照常可读，下面会说明登录后才能勾选。本地模式已自动签入，直接可用。

> **完成必须你自己确认。** 打开页面、点开链接都只算“开始”。
> 阶段完成需要你在“我的学习”里主动提交至少一条成果记录。这是刻意的设计，不是漏做。

### 课程目录 `/courses`

资料层，500+ 条目，**每页 24 条**，底部有分页。

- **筛选条**：学习轨道 / 访问方式 / 标签，选完点“应用筛选”。筛选写进 URL，可以直接分享。
- **卡片右上角**标出访问方式：`站内内容`（站内可读）、`本地优先`（本地模式可读）、`上游链接`（跳出去）、`待处理`。
- **卡片右下角**是解析后的实际动作，由 Content Resolver 统一决定——它会告诉你这一次点击到底会发生什么。

> **改坏了 URL 不会得到空页面。** 链接里出现下拉框里没有的轨道、访问方式或标签时，站点会忽略那一项而不是按它筛选，也不会把它回显到结果计数旁边。标签下拉只列真实的内容标签，导入时留下的记账标签（`legacy-reading`、`local`、`featured`）不出现在这里。

点标题进详情页 `/courses/<id>`：收录理由、访问入口、**相关入口**（该条目附带的 README、章节、GitHub 地址等），右栏是作者、许可证、复核状态和发布归属。

### 阅读器 `/read/<id>`

站内正文阅读。

- 右侧 **ON THIS PAGE** 是随页滚动的目录，长文会内部滚动。
- 本地素材顶部有一行归属说明：第三方素材、作者、许可证、上游链接。
- 有多个本地章节时，标题下方出现 **LOCAL CHAPTERS** 章节切换条。
- 正文末尾是**上一章 / 下一章**翻页；首章只有下一章，末章只有上一章。
- 素材缺失、格式不支持或路径不在白名单内时，页面会**明确说明原因**并给出上游回退入口，不会静默空白。

渲染是安全的：第三方 Markdown 里的排版 HTML（居中、徽章、表格）会保留，脚本、表单、内联事件一律丢弃。

> **为什么有些课只能读到一两章？**
> 站内可读的章节由课程条目**显式声明**，这是本地素材的安全边界。正文里指向未声明文件的链接会显示为普通文字而不是死链——不是坏了，是还没被收录。补齐声明属于内容策展工作（见 tasks.md T8.8）；在那之前，用条目页的**上游链接**读完整仓库。

### 搜索 `/search`

覆盖公开目录与允许访问的正文，可按关键词、阶段、轨道、访问方式组合过滤，同样分页。
本地模式下额外索引课程清单白名单里的章节。**私人笔记永远不进索引。**

每行结果左上角标出它是什么（`路线阶段` / `课程条目` / `本地章节` / `项目成果`），标题下面是定位信息：关联了路线阶段就显示阶段名，否则显示它属于哪个合集。

只有一个章节的课程条目在结果里**只出现一次**，那一章的正文并入该条目一起检索；真正分成多章的条目才会额外给出逐章结果，方便直接跳到某一章。

### 项目阶梯 `/projects`

两部分：

- **OPEN LADDER** — 11 个按难度递增的练习项目，与阶段解耦，想练哪个练哪个。
- **STAGE GATES** — 九个阶段各自要交的成果，点进去看具体阶段要求。

### 我的学习 `/learning`

私人区域。本地模式直接可用；云端模式需要先在 `/login` 用 GitHub 登录。

包含：

- **继续阅读** — 进行中的条目和上次的阅读位置。
- **阶段任务** — 按阶段分组的勾选清单，勾选即时保存。这里和阶段页勾的是同一份数据，两边看到的状态一致。
- **收藏** — 在课程页点“收藏”后汇总到这里。
- **私人笔记** — 按课程或阶段归档的 Markdown，只有你能看到，不进搜索。
- **阶段成果** — 提交仓库、演示或总结。**这里提交的记录，才是阶段完成的依据。**

右上角是两个导出操作：`导出 JSON`、`导出笔记 Markdown`。分隔线下面单独放着 `删除账户`——它是不可撤销的，所以刻意用不同颜色和位置与导出区分开，点击后仍会二次确认。

---

## 四、一条推荐动线

1. 首页 → **开始走路线**。
2. `/roadmap` 挑当前阶段（不确定就从 Stage 00 开始）；进度条会告诉你上次走到哪。
3. 阶段页读完 GOALS，照 PRACTICE TASKS 逐条做，做完一条就在原地勾掉。
4. 需要材料时点进**配套资料**，本地模式下直接在阅读器里读。
5. 边读边在“我的学习”记笔记；进度和阅读位置自动保存。
6. 做出东西后回到阶段页 → **去“我的学习”登记成果** → 提交仓库或演示链接。
7. 这一阶段才算完成，路线页那一行会变成 `已交成果`。进入下一站。

---

## 五、常见问题

**卡片写着“本地优先”，但打不开正文？**
你在云端模式。切到 `npm run dev:local --prefix code`，或确认素材文件确实在 `local-courses/` 下对应路径。

**“我的学习”一直转圈 / 勾选没反应？**
确认浏览器地址是 `127.0.0.1` 或 `localhost`，不是局域网 IP。开发模式下非回环来源的静态资源会被拒绝，页面无法完成 hydration。

**作者显示“作者待补”、许可证显示“许可证待确认”？**
这是如实呈现：这些条目从旧站导入时没带元数据，尚未人工复核。不是渲染错误。

**目录里 500 多条，看不过来？**
不要从目录开始。从 `/roadmap` 开始，只读当前阶段的配套资料——目录是查询层，不是阅读清单。

**能把本地素材发布到公网吗？**
不能。`local-courses/` 是第三方素材，本地副本不代表获得发布许可。云端镜像不打包、不代理、不索引它。详见[内容政策](docs/plans/plan.md)。

**手机上没有导航栏？**
窄屏下顶部导航变成一条可横向滑动的链接条，就在标题下方。首页的九阶段路线条同样可以横滑——两处的右边缘都会淡出，那就是“右边还有”的提示。

**顶部导航最后一项为什么不是“登录”？**
本地模式已经用固定单用户把你签进去了，所以那一项叫“账户”，点进去只是说明本机身份怎么存。云端模式下它才是“登录”。

---

## 六、维护者：改动过 local-courses 之后

素材库是你自己的目录，你随时会重组、增删它。站点不会自动跟着变——目录（`code/content/courses/courses.json`）里记的是路径，路径失效条目就打不开了。

改动之后跑一次对账：

```bash
npm run audit:materials --prefix code
```

它回答三个问题，报告写到 `code/reports/materials/catalog-drift.md`：

| 报告小节           | 说的是                                                  |
| ------------------ | ------------------------------------------------------- |
| Corroborated moves | 失效路径 + 可信的新位置（被多条路径共同印证的目录搬迁） |
| Needs a decision   | 判不准的和找不到的，只能你自己定                        |
| Uncatalogued       | 磁盘上有、目录里没有的仓库，附可粘贴的条目骨架          |
| Upstream fallback  | 没有上游回退的条目，按仓库分组                          |

先读报告，确认 Corroborated moves 那一栏没问题，再落盘：

```bash
npm run audit:materials --prefix code -- --apply
```

`--apply` **只**改写 Corroborated moves，判不准的一条都不碰。原因很实在：素材库有十几万个文件，被删掉的文件几乎总能在别处找到同名的，匹配器根本没有“已删除”这个答案。真发生过一次——LangChain 被整个删掉后，它的 `libs/README.md` 唯一匹配到了 DeepAgents 的同名文件。自动应用的话，那个条目会指向另一个项目的正文，页面照常渲染、检查全绿，只有你读到的东西是错的。

素材真的被删了、不是搬走了怎么办：把条目的 `localPath` 清空、`accessPolicy` 改成 `upstream-only`、补上 `sourceUrl`。条目本身要留着——云端目录从不依赖你本机有没有这份文件。

> 对账**不进** `npm run check`。整理素材目录不该让 typecheck 和测试跑不了。`audit:materials` 自己会非零退出，单独用。

---

## 七、维护者：走查与回归

两个脚本，分工不同：**`ui-review` 看页面长什么样，`functional-regression` 真的去点。**

先装一次 Playwright：

```bash
npm i -D playwright --prefix code && npx playwright install chromium
```

启动要测的模式，另开一个终端跑脚本：

```bash
npm run dev:local --prefix code
```

### UI 走查（版式）

```bash
node code/scripts/ui-review.mjs --base-url http://127.0.0.1:3000 --item-id legacy-course-001
```

在 desktop / tablet / mobile 三档抓全页截图，发现以下问题时非零退出：

- HTTP 4xx / 5xx
- 横向溢出（元素超出视口宽度）
- 列表页高度超过 12000px（通常意味着忘了分页；阅读器按内容长度豁免）
- 浏览器控制台报错

产物：`code/reports/ui-review/` 下的 `ui-review.md`、`ui-review.json` 和 `screenshots/`。

### 功能回归（点击）

```bash
node code/scripts/functional-regression.mjs --base-url http://127.0.0.1:3000
```

这个脚本会**真的操作站点**：遍历站内链接、翻页、应用筛选、切章节、点上下章、勾选任务、写删笔记、收藏再取消、拉导出接口。它会读页面上的模式徽标并据此断言——本地模式验证完整读写，云端模式验证匿名访问被正确拒绝、本地素材不出正文。脚本会清理自己创建的状态。

任何一项失败都会打印具体原因并非零退出，产物在 `code/reports/functional-regression/`。

两个脚本都是**维护命令，不是质量门禁**——它们需要运行中的服务和浏览器，所以 `npm run check` 不会调用它们。改完界面或交互后手动跑一次；两种模式都值得各跑一遍。

提交前仍然跑双模式门禁：

```bash
npm run check:cloud --prefix code
npm run check:local --prefix code
```

完整命令表见 [README](README.md#维护命令)。
