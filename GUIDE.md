# 使用指南

这份文档面向**使用者**：想按九阶段路线学 Agent 工程的人，以及在本机跑起这个站点的维护者。
需求与验收以 [spec.md](docs/plans/spec.md) 为准，架构与边界以 [plan.md](docs/plans/plan.md) 为准；本文只讲“怎么用”。

---

## 一、两种运行模式

站点只有两种模式，界面右上角的徽标随时告诉你当前在哪一种。

| |**云端模式** Cloud|**本地模式** Local|
|---|---|---|
|徽标|绿点 · 云端模式|橙点 · 本地模式|
|身份|GitHub 登录|回环地址上的固定单用户，免登录|
|能读什么|站内自有文章 + 上游链接|以上全部，**再加**本机 `local-courses/` 的素材正文|
|第三方素材|只显示作者、许可证和上游地址，不复制正文|只读渲染，可在站内直接阅读|
|学习状态|存在服务端 SQLite，跟随账户|存在本机 SQLite，属于这台机器|

一句话：**想真正读素材正文，用本地模式。**

---

## 二、把站点跑起来

需要 Node.js 22 或更新版本。

### 1. 安装依赖

```bash
npm ci --prefix code
```

### 2A. 本地模式（推荐，能读素材）

```bash
npm run dev:local --prefix code
```

打开 <http://127.0.0.1:3000>。右上角应显示**本地模式**。

素材目录默认取仓库根的 `local-courses/`。放在别处时指定：

```bash
LOCAL_MATERIAL_ROOT=/path/to/local-courses npm run dev:local --prefix code
```

### 2B. 云端模式（公开视角）

```bash
npm run dev:cloud --prefix code
```

这是部署到公网时的样子：第三方素材只给出处，不给正文。想验证“公开发布后别人看到什么”，用这个。

### 3. Docker 预览（最接近真实部署）

```bash
code/scripts/local-preview.sh
```

从当前代码构建镜像、启动本地模式、跑健康检查，然后打开 <http://127.0.0.1:3000>。
管理命令：`status`、`logs`、`restart`、`down`。`down` **不会**删除保存学习状态的 SQLite 卷。

> **只用 `127.0.0.1` 或 `localhost` 访问。** 本地模式的免登录身份只对回环地址成立，
> 站点在绑定到非回环地址时会直接拒绝启动。

---

## 三、页面怎么用

### 首页 `/`

三件事：九阶段路线的入口、四条轨道的分流、云端/本地两种模式的说明。
右侧深绿卡片是目录规模速览——**课程条目 / 本地素材 / 站内文章 / 上游导览**，四个数字相加等于总数。

### 九阶段路线 `/roadmap`

主线。每一站回答三个问题：要理解什么、要动手做什么、最后留下什么可复查的成果。
点进任一阶段 `/roadmap/stage-N`，会看到三栏：

1. **LEARNING GOALS** — 这一站要能说清楚的事。
2. **PRACTICE TASKS** — 编号动作，逐条做。
3. **PROOF OF WORK** — 深绿卡片写着这一站要交的东西，右下角直接跳到“我的学习”登记。

底部是这一站的**配套资料**，以及上一阶段 / 下一阶段导航。

> **完成必须你自己确认。** 打开页面、点开链接都只算“开始”。
> 阶段完成需要你在“我的学习”里主动提交至少一条成果记录。这是刻意的设计，不是漏做。

### 课程目录 `/courses`

资料层，500+ 条目，**每页 24 条**，底部有分页。

- **筛选条**：学习轨道 / 访问方式 / 标签，选完点“应用筛选”。筛选写进 URL，可以直接分享。
- **卡片右上角**标出访问方式：`站内内容`（站内可读）、`本地优先`（本地模式可读）、`上游链接`（跳出去）、`待处理`。
- **卡片右下角**是解析后的实际动作，由 Content Resolver 统一决定——它会告诉你这一次点击到底会发生什么。

点标题进详情页 `/courses/<id>`：收录理由、访问入口、**相关入口**（该条目附带的 README、章节、GitHub 地址等），右栏是作者、许可证、复核状态和发布归属。

### 阅读器 `/read/<id>`

站内正文阅读。

- 右侧 **ON THIS PAGE** 是随页滚动的目录，长文会内部滚动。
- 本地素材顶部有一行归属说明：第三方素材、作者、许可证、上游链接。
- 有多个本地章节时，标题下方出现 **LOCAL CHAPTERS** 章节切换条。
- 素材缺失、格式不支持或路径不在白名单内时，页面会**明确说明原因**并给出上游回退入口，不会静默空白。

渲染是安全的：第三方 Markdown 里的排版 HTML（居中、徽章、表格）会保留，脚本、表单、内联事件一律丢弃。

### 搜索 `/search`

覆盖公开目录与允许访问的正文，可按关键词、阶段、轨道、访问方式组合过滤，同样分页。
本地模式下额外索引课程清单白名单里的章节。**私人笔记永远不进索引。**

### 项目阶梯 `/projects`

两部分：

- **OPEN LADDER** — 11 个按难度递增的练习项目，与阶段解耦，想练哪个练哪个。
- **STAGE GATES** — 九个阶段各自要交的成果，点进去看具体阶段要求。

### 我的学习 `/learning`

私人区域。本地模式直接可用；云端模式需要先在 `/login` 用 GitHub 登录。

包含：

- **继续阅读** — 进行中的条目和上次的阅读位置。
- **阶段任务** — 按阶段分组的勾选清单，勾选即时保存。
- **收藏** — 在课程页点“收藏”后汇总到这里。
- **私人笔记** — 按课程或阶段归档的 Markdown，只有你能看到，不进搜索。
- **阶段成果** — 提交仓库、演示或总结。**这里提交的记录，才是阶段完成的依据。**

右上角三个操作：`导出 JSON`、`导出笔记 Markdown`、`删除账户`（不可撤销，会二次确认）。

---

## 四、一条推荐动线

1. 首页 → **开始走路线**。
2. `/roadmap` 挑当前阶段（不确定就从 Stage 00 开始）。
3. 阶段页读完 GOALS，照 PRACTICE TASKS 逐条做。
4. 需要材料时点进**配套资料**，本地模式下直接在阅读器里读。
5. 边读边在“我的学习”记笔记；进度和阅读位置自动保存。
6. 做出东西后回到阶段页 → **去“我的学习”登记成果** → 提交仓库或演示链接。
7. 这一阶段才算完成。进入下一站。

---

## 五、常见问题

**卡片写着“本地优先”，但打不开正文？**
你在云端模式。切到 `npm run dev:local --prefix code`，或确认素材文件确实在 `local-courses/` 下对应路径。

**“我的学习”一直转圈 / 勾选没反应？**
确认浏览器地址是 `127.0.0.1` 或 `localhost`，不是局域网 IP。开发模式下非回环来源的静态资源会被拒绝，页面无法完成 hydration。

**作者显示“作者待补”、许可证显示“许可证待确认”？**
这是如实呈现：这些条目从旧站导入时没带元数据，尚未人工复核。不是渲染错误。

**目录里 500 多条，看不过来？**
不要从目录开始。从 `/roadmap` 开始，只读当前阶段的配套资料——目录是查询层，不是阅读清单。

**能把本地素材发布到公网吗？**
不能。`local-courses/` 是第三方素材，本地副本不代表获得发布许可。云端镜像不打包、不代理、不索引它。详见[内容政策](docs/plans/plan.md)。

**手机上没有导航栏？**
窄屏下顶部导航变成一条可横向滑动的链接条，就在标题下方。

---

## 六、维护者：UI 走查

改动界面后，用走查脚本在三个视口回归检查：

```bash
npm i -D playwright --prefix code && npx playwright install chromium
```

```bash
npm run dev:local --prefix code
```

另开一个终端：

```bash
node code/scripts/ui-review.mjs --base-url http://127.0.0.1:3000 --item-id legacy-course-001
```

脚本会在 desktop / tablet / mobile 三档抓全页截图，并在发现以下问题时以非零码退出：

- HTTP 4xx / 5xx
- 横向溢出（元素超出视口宽度）
- 页面高度超过 12000px（通常意味着列表忘了分页）
- 浏览器控制台报错

产物在 `code/reports/ui-review/`：`ui-review.md`（人读）、`ui-review.json`（机读）、`screenshots/`（逐页截图）。
这是**维护命令，不是质量门禁**——它需要运行中的服务和浏览器，所以 `npm run check` 不会调用它。

提交前仍然跑双模式门禁：

```bash
npm run check:cloud --prefix code
npm run check:local --prefix code
```

完整命令表见 [README](README.md#维护命令)。
