# The application uses Better Auth's custom adapter over better-sqlite3 v13.
# Better Auth 1.6.26 declares an optional v12 peer for its built-in adapter;
# do not install that unused adapter or downgrade the application's SQLite driver.
legacy-peer-deps=true
